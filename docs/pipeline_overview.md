# AI Test Factory｜Pipeline Overview

---

# Core Pipeline

AI Test Factory 的核心目标：

是将测试问题，
逐渐转化为结构化测试资产。

系统主链路如下：

```text
Input
→ Parse
→ Structured Env
→ Prompt Build
→ AI Generate
→ Output Assets
→ Archive / Reuse
